from enum import Enum

class ThinkingType(Enum):
    """Enum for different types of thinking processes"""
    ANALYZE = "analyze"
    REFLECT = "reflect"
    CRITIQUE = "critique"
    INTEGRATE = "integrate"
    SYNTHESIZE = "synthesize"
    DELEGATE = "delegate"
    RESPOND = "respond" 